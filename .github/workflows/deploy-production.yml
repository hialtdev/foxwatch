name: Production Deploy

on:
  push:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Test · Build · Docker (production)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake make gcc g++ libssl-dev libcurl4-openssl-dev pkg-config

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo dependencies
        uses: Swatinem/rust-cache@v2

      - name: Check formatting (rustfmt)
        run: cargo fmt --all -- --check

      - name: Lint (clippy)
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test (release mode)
        run: cargo test --release --verbose

      - name: Build Docker image (production)
        run: docker build -t foxwatch:latest .

      # Uncomment when registry is configured
      # - name: Push to registry
      #   run: |
      #     echo "${{ secrets.REGISTRY_TOKEN }}" | docker login ghcr.io -u hialtdev --password-stdin
      #     docker tag foxwatch:latest ghcr.io/hialtdev/foxwatch:latest
      #     docker push ghcr.io/hialtdev/foxwatch:latest